<resources>
    <!-- Base application theme. -->
    <style name="Theme.FlashyAlarm" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FlashyAlarm.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FlashyAlarm.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FlashyAlarm.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
